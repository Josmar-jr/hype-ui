import type { Meta, StoryObj } from '@storybook/react'
import { Button } from '@hype-ui/react'

export default {
  title: 'Components/Button',
  component: Button,
} as Meta

export const Primary: StoryObj = {}
